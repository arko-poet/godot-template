Worked on:
- [x] add background music
- [x] mute setting
- [x] pausing game
- [x] scen loader
- [x] audio bus sliders

Ideas:
- separate settings into different kind of settings
- it would be nice if options UI didnt have to know the appropriate strings to fetch options from Settings
- add accept window popups
- quitting optipons menu should also quit pause menu
- options button pressed code duplication, how to handle it properly?
- pause menu export setting to choose between tranparent background and other
- scene laoder should have opption to cspawn loading screens
- make sure music keeps playing by dewfault when stuff is paused, proper music controller
- Settigns and ProjectSettings are confusing cause similar name - need to rethink it, maybe Options is a simple and good choice
- revisit naming of audio sliders, look for conventions in games
- volume sliders are not aligned properly, same for options
- investigate if volume sliders are more accessible if they are step based or more continous
- look into naming conventions for config files
- verify if in Settings autoload I am using StringNames where possible
- bus values in options_menu should be a loop
- think if there is a way of avoid null checks in optiuons_menu  ready
- set default options values for first time users
- investigating if updating volume with a slider too frequentyly is not causing any issues such as noise (especially on web builds)
- replace audio bus sliders from hardcoded to generated while also making a component for adding sliders automatically from the buses that exist
- update_bus_volume shjould be called set_bus_volume
- _initialist_from_config is also a confusing name
- consider modifying settings to have variable for every setting