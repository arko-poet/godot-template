Work done
- [x] link options with main menu
	- how should it work?
		- option 1: Node instantiate options scene and free it
		- option 2: Control hide and show it as needed
		- option 3: dedicated SceneLoader
- [x] add credits scene and link it to main menu
- [x] add example game scene and link it to main menu

Idea Dump:
- options menu could remember previous options and allow user to accept changes, revert changes or restart to defaults
- use diffent options menu UI layout so it doesnt look like maaacks
- allow tempalte to be built automatically by export variables that define content as well as sizing
- add template and engine to credits
- use ui generic input to navigate ui, e.g. press escape to go back, enter to accept
- add ui navigation through generic ui input, e.g. press down arrow to move down
- ensure no ui button collides with itch full screen button