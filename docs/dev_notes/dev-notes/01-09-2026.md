Worked on:
- [x] refactoring settings to use variables instead of reading from config every time
- [x] refactor audio bus sliders to be built automatically per bus
- [x] audio bus component
- [x] async scene loading with a lodaing screen

Ideas:
- bool var convetions for names
- godot does variabler name in setter always should be value
- credits should have black background
- is it fullscreen or full screen, change apprporiate references if code if needed
- consider using dedicated component for audio bus sliders instead of coding them manually
- rework settings to use dedicated components in a vbox instead of grid
- maybe specifying volume value in addition to slider is a good idea
- add instructions to documentation on how to add more audio sliders/buses
- make areusable components for options for sliders and toggles